# Allow local connections (Unix socket)
local   all             all                     trust :contentReference[oaicite:13]{index=13}

# Allow TCP connections from any IP to all DBs/users
host    all             all             0.0.0.0/0       md5   :contentReference[oaicite:14]{index=14}

# Permit standby to authenticate when fetching WAL
host    replication     replicator      0.0.0.0/0       md5   :contentReference[oaicite:15]{index=15}
